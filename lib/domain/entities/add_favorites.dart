class AddFavorites {
    final String message;

    AddFavorites({
        required this.message,
    });
}

class IsFavorites {
    final bool message;

    IsFavorites({
        required this.message,
    });
}